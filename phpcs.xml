<?xml version="1.0"?>
<ruleset name="PHP-Trace Coding Standard">
    <description>PSR-12 coding standard for PHP-Trace project</description>

    <!-- Show progress -->
    <arg value="p"/>
    <!-- Show sniff codes in all reports -->
    <arg value="s"/>
    <!-- Use colors in output -->
    <arg name="colors"/>

    <!-- Check these directories -->
    <file>src</file>
    <file>tests</file>
    <file>examples</file>

    <!-- Use PSR-12 as the base standard -->
    <rule ref="PSR12">
        <!-- Allow snake_case test method names -->
        <exclude name="PSR1.Methods.CamelCapsMethodName.NotCamelCaps"/>
    </rule>

    <!-- Exclude vendor directory -->
    <exclude-pattern>*/vendor/*</exclude-pattern>
    <exclude-pattern>*/cache/*</exclude-pattern>
    <exclude-pattern>*/coverage/*</exclude-pattern>

    <!-- Examples can have side effects -->
    <rule ref="PSR1.Files.SideEffects.FoundWithSymbols">
        <exclude-pattern>examples/*</exclude-pattern>
    </rule>
</ruleset>
